<!-- ナビゲーション（ほぼ共通） -->
<nav class="navbar navbar-expand-lg navbar-dark bg-info fixed-top">
  <a class="navbar-brand" href="#">Book Search</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false"
        aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <% if page_category == 'top' %>
          <a class="nav-link active" href="/">トップ</a>
        <% else %>
          <a class="nav-link" href="/">トップ</a>
        <% end %>
      </li>
      <li class="nav-item">
        <% if page_category == 'search' %>
          <%= link_to '書籍検索', search_index_path, :class => "nav-link active" %>
        <% else %>
          <%= link_to '書籍検索', search_index_path, :class => "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item">
        <% if page_category == 'mypage' %>
          <%= link_to 'マイページ', mypage_index_url, :class => "nav-link active" %>
        <% else %>
          <%= link_to 'マイページ', mypage_index_url, :class => "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item">
        <% if page_category == 'manage' %>
          <%= link_to '管理画面', manage_console_index_url, :class => "nav-link active" %>
        <% else %>
          <%= link_to '管理画面', manage_console_index_url, :class => "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        ユーザー</a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if @current_user %>
            <%= link_to '編集', edit_user_path(@current_user), class: 'dropdown-item' %>
            <%= link_to 'ログアウト', logout_path, method: :get, class: 'dropdown-item' %>
          <% else %>
            <%= link_to '登録', new_user_path, class: 'dropdown-item' %>
            <%= link_to 'ログイン', root_path, class: 'dropdown-item' %>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
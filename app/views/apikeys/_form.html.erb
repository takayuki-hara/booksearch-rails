<%= form_with(model: apikey, local: true) do |form| %>
  <% if apikey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apikey.errors.count, "error") %> prohibited this apikey from being saved:</h2>

      <ul>
      <% apikey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_field :key %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
